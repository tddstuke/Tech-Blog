<div>
  {{#each this}}
    <section class="box m-5 has-background-grey-light">
      <div>
        {{user.username}}
        on
        {{format_date created_at}}
      </div>
      <div class="text">
        {{comment_text}}
      </div>
    </section>
  {{/each}}
</div>